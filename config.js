/* ============================================================
   Kinora runtime config.
   Real accounts are ENABLED (Supabase keys are set below).
   The anon/publishable key is safe to expose in client code —
   it only works through Row Level Security (RLS) on your tables.

   To change projects: Supabase dashboard -> Project Settings -> API
   ============================================================ */
window.KINORA_CONFIG = {
  SUPABASE_URL: "https://liogdrivueybgbbnydsg.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_Fj1bvcD4oDE6MCZtQV4vwA_NlJQQPqc"
};
